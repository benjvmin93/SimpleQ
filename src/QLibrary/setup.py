from setuptools import setup

setup(
    name='SimpleQ',
    version='0.0.1',
    description='Emulator Packages for the API',
    author='BM/BG',
    author_email='X',
    packages=['SimpleQ'],
    install_requires=[],
)
